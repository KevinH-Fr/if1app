<%= form_with(model: parisbi) do |form| %>
  <% if parisbi.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(parisbi.errors.count, "error") %> prohibited this parisbi from being saved:</h2>

      <ul>
        <% parisbi.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
