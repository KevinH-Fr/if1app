<% if user_signed_in? %>
  <% if current_user.admin? %>

    <h1>Nouvelle equipe</h1>

    <%= render "form", equipe: @equipe %>
    <br>
    <div>
      <%= link_to "Retour equipes", equipes_path %>
    </div>

  <% end %>
  <br/>
  <strong>Vous n'avez pas les droits...</strong>
<% end %>