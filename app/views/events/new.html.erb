<% if user_signed_in? %>
  <% if current_user.admin? %>

    <h1>Nouvel event</h1>
    <%= render "form", event: @event %>

    <br>
    <div>
      <%= link_to "Retour events", events_path %>
    </div>

  <% end %>
  <br/>
  <strong>Vous n'avez pas les droits...</strong>
<% end %>